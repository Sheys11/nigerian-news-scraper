[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt && playwright install-deps && playwright install chromium && playwright install firefox"

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
